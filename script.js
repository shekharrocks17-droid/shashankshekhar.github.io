const menu=document.getElementById('menu'),nav=document.querySelector('nav');if(menu)menu.onclick=()=>nav.classList.toggle('open');
const release=new Date('2026-10-25T00:00:00+05:30');function tick(){const e=document.getElementById('count');if(!e)return;let x=Math.max(0,release-new Date()),d=Math.floor(x/86400000);x%=86400000;let h=Math.floor(x/3600000);x%=3600000;let m=Math.floor(x/60000);let s=Math.floor(x/1000)%60;e.innerHTML=`<span><b>${d}</b><small>Days</small></span><span><b>${String(h).padStart(2,'0')}</b><small>Hours</small></span><span><b>${String(m).padStart(2,'0')}</b><small>Min</small></span><span><b>${String(s).padStart(2,'0')}</b><small>Sec</small></span>`}tick();setInterval(tick,1000);
document.querySelectorAll('.buy').forEach(b=>b.onclick=()=>{alert('Razorpay checkout will be connected here. This button is ready for your Razorpay Payment Link/Checkout integration.');});function subscribe(e){e.preventDefault();document.getElementById('submsg').textContent='Signup form ready — connect your email service to receive subscribers.'}function contactForm(e){e.preventDefault();document.getElementById('contactmsg').textContent='Contact form ready — connect it to your email/form service.'}
